{{> header}}
{{> navbar}}
<main class="bg-dark">

{{!-- collections --}}
<div class="container-fluid bg-dark">
  <div class="container text-xenter text-light">
      <div class="row row-cols-1 row-cols-md-3 pt-5 pb-3 text-center">
        {{#each collections}}
            <div class="col">
              <div class="card mb-4 rounded-3 shadow-sm bg-secondary">
                <div class="card-header py-3">
                  <h4 class="my-0 fw-normal">{{this.name}}</h4>
                </div>
                <div class="card-body">
                  <a class="w-100 btn btn-lg btn-primary" href="/detailCollection/{{this.id}}">detail</a>
                </div>
              </div>
            </div>
        {{/each}}
      </div>
      <div class="row row-cols-1 row-cols-md-3 pb-3 text-center">
        <div class="col">
          <div class="card mb-4 rounded-3 shadow-sm bg-secondary">
            <div class="card-header py-3">

            </div>
            <div class="card-body">
              <a class="w-100 btn btn-lg btn-primary" href="/addCollections"> add collections</a>
            </div>
          </div>
        </div>
      </div>
  </div>
</div>

</main>
</div>

{{> footer}}